# Sine Wave - Large Dataset
# ==========================
# Pattern: Extended oscillation for longer training
# Use: Preventing overfitting with TD3/off-policy algorithms

data_generator:
  pattern_type: "sine_wave"
  n_periods: 10  # More pattern repetitions
  samples_per_period: 500  # Smoother patterns
  n_samples: 5000  # 10x more data prevents overfitting
  base_price: 50000.0
  amplitude: 8000.0
  trend_slope: 0.0  # No trend - pure oscillation
  volatility: 0.0002
  start_date: "2024-01-01"
